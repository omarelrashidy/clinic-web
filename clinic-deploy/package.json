{
  "name": "dr-ahmed-salem-clinic",
  "version": "1.0.0",
  "private": true,
  "description": "Clinic booking system (client + staff dashboard) backed by Google Apps Script",
  "scripts": {
    "build": "echo 'Static site — no build step required'",
    "start": "npx serve public"
  }
}
